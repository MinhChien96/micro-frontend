import { setupStandaloneSession } from '@app/common/mocks/standalone';
import { type CSSProperties, useEffect } from 'react';
import {{pascalCase name}}App from '../components/{{pascalCase name}}App';

// Trang standalone dev: chạy MFE độc lập không cần shell
//   pnpm --filter ./remotes/mfe-{{kebabCase name}} start
const pageStyle: CSSProperties = { padding: 24 };
const bannerStyle: CSSProperties = {
  marginBottom: 16,
  padding: '8px 14px',
  borderRadius: 8,
  background: '#dcfce7',
  color: '#166534',
  fontSize: 12,
  fontWeight: 600,
};

export default function Page() {
  useEffect(() => {
    setupStandaloneSession('PREMIUM');
  }, []);

  return (
    <div style={pageStyle}>
      <div style={bannerStyle}>
        Standalone — mfe-{{kebabCase name}} :{{port}} · Modern.js · Mock: PREMIUM role
      </div>
      <{{pascalCase name}}App />
    </div>
  );
}
